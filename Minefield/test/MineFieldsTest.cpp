#include "gmock/gmock.h"
#include "MineFields.h"
#include <string>

using namespace ::testing;

using namespace std;

TEST(MineFields, Draw)
{
	/*
	MineFields mines;

	string board = "4 4 ";

	string result = mines.drawBoard(board);

    ASSERT_EQ("4 4 ", result);
    */
}
